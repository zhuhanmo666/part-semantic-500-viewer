# 3D Part Semantic Dataset Viewer

A self-contained static site for 500 quality-passed GLB models with complete part semantics. Serve this directory over HTTP or publish its contents from the root of a GitHub Pages repository.

Local preview: `python -m http.server 8099 --directory .`
